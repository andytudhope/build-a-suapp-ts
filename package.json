{
  "name": "build-a-suapp",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "compile": "cd contracts && forge build"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.2.2",
    "vite": "^5.0.0"
  }
}